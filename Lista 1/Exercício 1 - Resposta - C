//Exercicio1
#include <stdio.h>

int main() {
    int turma, c = 0;
    float notas, media, maiorMedia, menorMedia;

    do {
    printf("Entre com o numero de alunos da turma: ");
    
    scanf("%d", &turma);
    printf("\n");
    if (turma <= 0 || turma > 50) {
        printf("O numero de alunos na turma deve ser maior do que zero e menor ou igual a 50");
        printf("\n");
    }
    printf("\n");
}   while (turma <= 0 || turma > 50);


    while (c < turma) {
        c++;
        printf("Notas aluno %d: ", c);
        scanf("%f", &notas);
        printf("\n");

//Calculo da media virá aqui
        printf("Media final do aluno %d: \n", c);
        printf("\n");
    }

    printf("Media geral da turma: %.2f\n", media);
    printf("\n");

    printf("Maior media da turma: %.2f (Alunos: %d, %d)\n", maiorMedia, 1, 4);
    printf("\n");

    printf("Menor media da turma: %.2f (Alunos: %d)\n", menorMedia, 2);
    printf("\n");

    return 0;
}
